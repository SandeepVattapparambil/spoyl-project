<!DOCTYPE html>
<html>

<head>
  <title>
    <%= title %>
  </title>
  <link href="https://fonts.googleapis.com/css?family=Roboto:300,300i,400" rel="stylesheet">
  <link rel='stylesheet' href='stylesheets/style.css' />
</head>

<body>
  <div class="header">
    <span>Spoyl-SSR-Project</span>
  </div>
  <div class="main">
    ​
    <div class="container">
      <div class="group">
        <form method='POST' action='/post'>
          <input type="text" name="searchTerm" required>
          <button class="btn" type="submit">
            <span>Search</span>
          </button>
          <span class="bar"></span>
        </form>
      </div>
    </div>
    <div class="container-grid">
    <% for(var i=0; i<imageData.length; i++){ %>
      <div class="holder holder-1">
      <img src=<%= imageData[i].urls.thumb %> class="thumb">
        <span class="likes"><%= imageData[i].likes %> Likes</span>
        <span class="user"><%= imageData[i].user.name %> </span>
        <img class="profilePic" src=<%= imageData[i].user.profile_image.medium %>>
      </div>
        
        <% }%>
    </div>
  </div>
</body>

</html>